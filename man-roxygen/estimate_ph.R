#' @param estimate Whether the estimate (corresponding to the estimated mean 
#'   difference between the levels if \code{pairwise = TRUE} and the estimated 
#'   marginal means if \code{pairwise = FALSE}) should be displayed.
