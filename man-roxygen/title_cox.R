#' @param title Title that is used as the caption of the LaTeX table. 
#'   If \code{title = NULL} then the caption is ”Cox Regression” if 
#'   \code{results = "summary"} and ”Cox Analysis of Variance” if 
#'   \code{results = "Anova"}.
