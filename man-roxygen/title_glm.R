#' @param title Title that is used as the caption of the LaTeX table. 
#'   If \code{title = NULL} then the caption is ”Logistic Regression” if 
#'   \code{results = "summary"} and ”Binomial Analysis of Variance” if 
#'   \code{results = "Anova"}.
