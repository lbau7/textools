#' @param title Title that is used as the caption of the LaTeX table. 
#'   If title is NULL then the caption is ”Logistic Mixed Model Regression” 
#'   if \code{results = "summary"} and ”Binomial Mixed Model ANOVA” if 
#'   \code{results = "Anova"}.
